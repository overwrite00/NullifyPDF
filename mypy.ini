[mypy]

# "fitz" is a thin alias package for PyMuPDF that ships no py.typed marker,
# so mypy cannot analyze it (the CI run passes --ignore-missing-imports for the same reason).
[mypy-fitz.*]
ignore_missing_imports = True
